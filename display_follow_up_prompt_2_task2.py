def get_follow_up_prompt_2_task2():
    prompt = """Please activate the 'Layout' selector in the sidebar. When a user selects 'Modern', 'Classic', or 'Minimal', the main font preview area (e.g., the `.font-display` container or the `.font-box` elements) should visually change in a simple way to acknowledge the selection. For example, you could change its border style, add a subtle background color, or slightly alter its padding for each layout option to show the selection is working."""
    
    print("Follow-up Prompt 2 (Task 2 - Typography):")
    print("---")
    print(prompt)
    print("---")

if __name__ == "__main__":
    get_follow_up_prompt_2_task2()
